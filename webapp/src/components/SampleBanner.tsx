interface SampleBannerProps {
  sample?: any;
  onScenario?: (id: any) => void;
}

export function SampleBanner(_props: SampleBannerProps) {
  // Completely disabled and removed as per user design specification
  return null;
}
